(defwidget icon [icon ?cmd ?tooltip ?class ?theme]
    (button
        :class
            'icon 
            ${theme} 
            ${style} 
            ${class}'
        :onclick "${cmd}"
        :tooltip "${tooltip}"
        "${icon}"))
